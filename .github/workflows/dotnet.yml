# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal

  code_quality:

    runs-on: windows-latest

    steps:
        - name: dotnet-sonarscanner
  # You may pin to the exact commit or the version.
  # uses: Secbyte/dotnet-sonarscanner@10230bbd60c5b727532504308cba3c6577deba14
          uses: Secbyte/dotnet-sonarscanner@v2.3
          with:
            # TODO: Command to invoke build
            buildCommand: 
            # TODO: Command to invoke tests
            testCommand: # optional
            # TODO: Specifies the key of the analyzed project in SonarQube
            projectKey: 
            # TODO: Specifies the name of the analyzed project in SonarQube
            projectName: 
            # ? The server URL
            sonarHostname: # optional, default is https://sonarcloud.io
            #  ? Organisation
            sonarOrganisation: 
            # ? Arguments to append to the begin command
            beginArguments: # optional
            # ? Arguments to append to the end command
            endArguments: # optional
          
